CREATE DATABASE SistemaClientes;

-- Seleciona o banco de dados para uso
USE SistemaClientes;

-- Cria a tabela de Clientes
CREATE TABLE Clientes (
    id_cliente INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    data_cadastro TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Cria a tabela de Pedidos
CREATE TABLE Pedidos (
    id_pedido INT AUTO_INCREMENT PRIMARY KEY,
    id_cliente INT,
    valor_total DECIMAL(10,2) NOT NULL,
    data_pedido DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_cliente) REFERENCES Clientes(id_cliente)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

-- Exemplo de inserção de dados na tabela de Clientes
INSERT INTO Clientes (nome, email) 
VALUES 
    ('Ana Souza', 'ana@email.com'),
    ('Bruno Silva', 'bruno@email.com');

-- Exemplo de inserção de dados na tabela de Pedidos
INSERT INTO Pedidos (id_cliente, valor_total) 
VALUES 
    (1, 150.50),
    (2, 200.00);

-- Consulta para visualizar os dados
SELECT c.nome, p.valor_total, p.data_pedido
FROM Clientes c
INNER JOIN Pedidos p ON c.id_cliente = p.id_cliente;
